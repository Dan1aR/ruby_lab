<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
<title>Главная страница</title>
<meta name='viewport' content='width=device-width,initial-scale=1,minimum-scale=1,maximum-scale=1,user-scalable=no'/>
<meta name="description" content="">
<meta name="keywords" content="">
<link href="" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
    <div class="container">
      <header class="blog-header py-3">
        <div class="row flex-nowrap justify-content-between align-items-center border-bottom">
          <div class="col-4 pt-1">
            
            </div>
            <div class="col-4 text-left">
              <h4 class="blog-header-logo text-dark" href="#"><%= t 'posts.show.hello' %></h4>
            </div>
            <% if signed_in? %>
              <div class="col-2 d-flex justify-content-end align-items-center">
                <a class="btn btn-sm btn-outline-secondary" href=<%=session_update_path%>><%= current_user.username%></a>
                |
                <a class="btn btn-sm btn-outline-secondary" href=<%=session_logout_path%>><%= t 'posts.show.logout' %></a>
              </div>
            <% else %>
              <div class="col-4 d-flex justify-content-end align-items-center">
                <a class="btn btn-sm btn-outline-secondary" href="<%=session_login_path%>"><%= t 'posts.show.login' %></a>
              </div>
            <% end %>

        </div>
      </header>
    </div>

    <div class="container">
      <div class="col-md-12">
        <div class="card flex-md-row mb-4 box-shadow h-md-250">
          <div class="card-body d-flex flex-column align-items-start">
            <h3 class="mb-0">
              <h3 class="text-dark"> <%= @post.title %> </h3>
            </h3>
            <div class="mb-1 text-muted"> <%= @post.created_at.strftime("%d-%m-%Y") %> : <%= @post.user_id%> </div>
            <p class="card-text mb-auto"><%= @post.text %></p>
        </div>
      </div>
      <% if @post.avatar.attached? %>
        <img class="card-img-top" src="<%= url_for(@post.avatar) %>">
      <% end %>
    </div>

    <% if @post.user_id == current_user[:username] %>
      <%= link_to t('posts.show.edit'), edit_post_path(@post) %>
    <% end %>
    |
    <%= link_to t('posts.show.back'), posts_path %>

    
</body>
</html>